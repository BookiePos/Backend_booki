{
  "name": "erp-backend",
  "version": "0.1.0",
  "private": true,
  "description": "API del ERP (restaurante -> retail). Node/TS + MongoDB.",
  "type": "module",
  "scripts": {
    "dev": "echo \"TODO: configurar servidor (NestJS recomendado)\" && exit 0",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"TODO: configurar test runner (vitest/jest)\" && exit 0",
    "lint": "echo \"TODO: eslint\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
